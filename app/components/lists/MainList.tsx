
import type MainListProps from "./MainListProps";

export default function MainList({params} : {params: MainListProps}) {
    return (
        <div className="flex flex-1 h-full w-full bg-gray-800 text-white">
            Main List
        </div>
    )
}

