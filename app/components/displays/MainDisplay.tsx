
import type MainDisplayProps from "./MainDisplayProps";

export default function MainDisplay({params} : {params: MainDisplayProps}) {
    return (
        <div className="flex flex-1 h-full w-full bg-gray-800 text-white">
            Main Display
        </div>
    )
}

